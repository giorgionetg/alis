---
import '@/styles/global.css';
import 'animate.css';
import { site } from '@/config';
import Slide from '@/components/Slide.astro';
import CookieConsent from '@/components/CookieConsent.astro';
import Footer from '@/components/Footer.astro';
---

<!doctype html>
<html lang='en'>
  <head>
    <meta charset='UTF-8' />
    <meta name='viewport' content='width=device-width' />
    <link rel='icon' type='image/svg+xml' href='/favicon.svg' />
    <title>{site.fullname} | {site.payoff}</title>
    <meta name='author' content={site.fullname} />
    <meta name='generator' content='Made with ❤️ by Giorgio Tedesco' />
    <!-- Developed by Giorgio Tedesco | giorgiotedesco.it -->
    <script defer src="https://get.giorgiotedesco.it/script.js" data-website-id="04e978e4-8231-4926-8b38-22c2a45666f3"></script>
    <script type="module">
      import { initLibp2p, callBackend } from '/js/libp2p.js'
      initLibp2p();

      const BACKEND = "/dns4/relay.giorgiotedesco.it/tcp/443/wss/p2p/12D3KooWNkNn6YEcrRxpW1SJxBDMT8XxcGk9wRMPUDCGtXqXiyP7";
      //await callBackend(BACKEND, { method: 'ping' });
      document.addEventListener('DOMContentLoaded', async () => {
        //const pong = await callBackend(BACKEND, { method: 'ping' });
        //console.log("📡 Ping:", pong);

        try {
          const pong = await callBackend(BACKEND, { method: 'ping' });
          console.log("✅ Ping ricevuto:", pong);
        } catch (err) {
          console.error("❌ Errore ping:", err)
        }
      })



    </script>
  </head>
  <body>
    <slot />
    
    <Footer />
  </body>
</html>

<style>
  html,
  body {
    margin: 0;
    width: 100%;
    height: 100%;
  }
</style>
